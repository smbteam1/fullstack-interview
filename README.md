# Project Issues

This repository currently contains the following issues:

- **Port conflict:** The frontend and backend are both configured to use port `3000`.
- **Missing environment configuration:** No `.env` or environment configuration files are used.
- **Google AI mode used:** The application is made with help of Google AI mode.
